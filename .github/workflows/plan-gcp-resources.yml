---
name: "Plan GCP Resources"

on:
  pull_request:
    paths:
      - "terraform-config/**"

jobs:
  terraform:
    name: "Terraform Plan"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3
